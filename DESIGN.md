# راهنمای طراحی — سامانه مدیریت پروژه‌های بانک سپه

مرجع سیستم طراحی این پروژه؛ برای توسعه‌دهندگانی که کامپوننت یا صفحه جدید اضافه می‌کنند.
سبک بصری کلی: **glassmorphism تیره/روشن با گرادیان آبی-نارنجی بانک سپه، راست‌به‌چپ (RTL) و اعداد فارسی.**

## نقشه فایل‌ها

| فایل | مسئولیت |
|---|---|
| [app/globals.css](app/globals.css) | توکن‌های رنگی، تایپوگرافی، همهٔ کلاس‌های صفحات و کامپوننت‌ها |
| [app/styles/logo.css](app/styles/logo.css) | فقط استایل‌های نشان/لوگوی بانک سپه (استخراج‌شده از globals.css) |
| [app/lib/date.ts](app/lib/date.ts) | تبدیل تاریخ شمسی/میلادی و ارقام فارسی |
| [app/lib/permissions.ts](app/lib/permissions.ts) | قانون سطح دسترسی (`canManage`) |
| [app/components/](app/components) | کامپوننت‌های مشترک (مودال‌ها، بخش‌های منشور، نمودارها) |
| [public/](public) | فونت IRANSans و تصاویر نشان بانک |

## هویت بصری و لوگو

جزئیات کامل در [app/styles/logo.css](app/styles/logo.css). خلاصه:

| دارایی | ابعاد اصلی | محل استفاده |
|---|---|---|
| `mob.banking.android.sepah_512x512.webp` | ۲۵۶×۲۵۶ | نشان کوچک مربعی — صفحه بارگذاری، نوار کناری، لوگوی موبایل صفحه ورود |
| `sepah-emblem.png` | ۳۳۳×۳۸۰ | نشان بزرگ تشریفاتی — واترمارک پس‌زمینه صفحه ورود |
| `images.jpg` | ۴۴۷×۴۴۷ | لوگوی مربعی ثانویه — بالای پنل برند صفحه ورود |

قاعده: اندازهٔ نمایش با CSS (`object-fit: cover`) کنترل می‌شود، نه با تغییر ابعاد خود فایل.

## توکن‌های رنگی

تعریف‌شده در `:root` در `app/globals.css`:

```css
--navy-950: #061c2b;   /* پس‌زمینه تیره‌ترین */
--navy-900: #08283a;
--navy-800: #0b3549;
--blue-600: #0877a7;   /* رنگ اصلی برند / لینک‌ها / دکمه اصلی */
--blue-500: #1594c5;
--cyan-300: #62c6dc;   /* هایلایت گرادیان */
--orange-500: #ed6b2d; /* رنگ دوم برند (لهجه/تاکید) */
--ink: #102c3b;        /* متن اصلی روی زمینه روشن */
--muted: #6e8490;      /* متن کم‌رنگ */
--line: rgba(25, 79, 99, .12); /* خط جداکننده */
--glass: rgba(255, 255, 255, .78);
```

### رنگ‌های وضعیت (status tones)

این چهار تن در سراسر برنامه برای نشان‌دادن وضعیت به‌کار می‌روند (پیل‌های وضعیت، آیکن‌های stat، نمودارها):

| تن | رنگ متن | رنگ زمینه | معنا |
|---|---|---|---|
| `teal` | `#16866e` / `#168a72` | `#e3f5ef` | مثبت / کم‌ریسک / تکمیل‌شده |
| `blue` | `#1b7aa1` / `#187eac` | `#e3f1f8` | خنثی / اطلاعاتی |
| `amber` | `#b9751b` / `#c17a1e` | `#fff0d6` | هشدار / درجریان / متوسط |
| `rose` | `#bd5050` / `#c95555` | `#fde6e5` | بحرانی / رد‌شده / پرریسک |

این چهار تن روی کلاس‌های `.status-pill.*`، `.stat-icon.*`، `.rpn-pill.*`، `.activity-icon.*` و `.risk-heatmap-cell.*` تکرار می‌شوند — برای رنگ جدید هرجا لازم شد از همین چهارتایی استفاده کنید، رنگ اختراع نکنید.

### رنگ RPN (ریسک × احتمال × شدت)

```
RPN ≥ 20  → rose  (بحرانی)
RPN ≥ 10  → amber (متوسط)
RPN < 10  → teal  (کم)
```
منطق در `rpnTone()` داخل [RiskHeatmap.tsx](app/components/charts/RiskHeatmap.tsx) و به‌صورت inline در `page.tsx` تکرار شده — هر تغییری در آستانه باید در هر دو جا اعمال شود.

### پالت نمودارها

`WbsWeightDonut` و نمودارهای دسته‌ای از یک پالت هشت‌رنگی ثابت استفاده می‌کنند (`PALETTE` در [WbsWeightDonut.tsx](app/components/charts/WbsWeightDonut.tsx)):
`#1594c5 #20a0bd #ed6b2d #8ad7e6 #c17a1e #5c7280 #43d49f #c95555`

## تایپوگرافی

- فونت: **IRANSans** (`public/fonts/IRANSans-Medium.ttf`, وزن ۵۰۰)، جایگزین: Tahoma, sans-serif.
- جهت سند: `dir="rtl"` در `app/layout.tsx`.
- اعداد همیشه فارسی نمایش داده می‌شوند: از `toFaDigits()` (تعریف محلی در `page.tsx` و نسخهٔ مشترک در `app/lib/date.ts`) استفاده کنید، هرگز رقم لاتین مستقیم در JSX ننویسید.
- اندازه فونت با `px` مطلق تعریف می‌شود (مقیاس معمول UI بین ۷px تا ۲۵px برای این طرح فشرده)؛ از واحد `rem` استفاده نشده است — با همین قرارداد پیش بروید.

## الگوی شیشه‌ای (glass-card)

پایه تمام کارت‌ها:

```css
.glass-card {
  background: linear-gradient(145deg, rgba(255,255,255,.76), rgba(255,255,255,.42));
  border: 1px solid rgba(255,255,255,.88);
  box-shadow: 0 24px 65px rgba(8,40,58,.11), inset 0 1px 0 rgba(255,255,255,.96), inset 0 -1px 0 rgba(36,117,146,.06);
  backdrop-filter: blur(28px) saturate(155%);
}
```

نوار کناری (`.sidebar`) و مودال‌ها نسخهٔ تیره‌تر همین الگو را با شفافیت و blur بیشتر دارند. هر container جدید که «کارت» است باید کلاس `glass-card` بگیرد، نه یک پس‌زمینه دلخواه.

### شعاع گردی (border-radius)

مقیاس تقریبی مشاهده‌شده — از این پله‌ها استفاده کنید، بین آن‌ها گرد نکنید:

`7–9px` (پیل/بج کوچک) → `10–14px` (دکمه/ورودی) → `18–22px` (کارت/section) → `27–32px` (پنل بزرگ/سایدبار/مودال ورود) → `999px` (پیل کامل/دایره).

## کامپوننت‌های پایه (اینونتوری)

| کلاس/کامپوننت | استفاده |
|---|---|
| `.stat-card` | کارت شاخص عددی در بالای داشبورد |
| `.status-pill` | برچسب وضعیت کوچک (تن‌های بالا) |
| `.rpn-pill` | برچسب RPN ریسک |
| `.data-table` / `.data-table-wrap` | جدول‌های منشور پروژه (اسکرول افقی خودکار) |
| `.gauge-ring` | حلقهٔ درصد پیشرفت با `conic-gradient` |
| `.empty-state` | حالت خالی (آیکن + متن وسط‌چین) |
| `.readonly-badge` | نشان "فقط مشاهده" برای کاربران بدون دسترسی مدیریت |
| `.modal-backdrop` / `.modal-panel` | الگوی پایهٔ مودال (اعلان‌ها، تبدیل تاریخ) |
| نمودارهای SVG اینلاین | `donut-*`, `risk-heatmap-*`, `gantt-*` — بدون کتابخانهٔ نموداری خارجی، برای سبک‌ماندن باندل |

اصل کلی: **بدون کتابخانهٔ چارت جدید.** نمودارهای این پروژه (donut وزنی WBS، heatmap ریسک، Gantt) عمداً SVG خام‌اند تا هیچ وابستگی سنگین (مثل chart.js) اضافه نشود؛ کامپوننت نمودار جدید هم به همین روش (SVG + توکن‌های رنگی بالا) بسازید.

## چاپ (Reports)

بخش گزارش‌ها (`app/components/ReportsSection.tsx`) از `window.print()` استفاده می‌کند. قوانین چاپ در `@media print` انتهای `globals.css`: سایدبار/تاپ‌بار/دکمه‌های تعاملی مخفی می‌شوند، کارت‌های `glass-card` به حاشیهٔ ساده تبدیل می‌شوند (بدون blur/سایه). هر بخش قابل‌چاپ جدید باید کلاس `.report-print-block` بگیرد و هر چیز غیرچاپی کلاس `.no-print`.

## واکنش‌گرایی (Breakpoints)

| نقطه شکست | تغییر اصلی |
|---|---|
| `1100px` | ستون‌های grid داشبورد/آمار به ۲ ستون |
| `900px` | نمودارهای گزارش (`.report-charts-grid`) تک‌ستونه |
| `820px` | نوار کناری off-canvas می‌شود، پنل برند صفحه ورود مخفی و لوگوی موبایل ظاهر می‌شود |
| `700px` | ردیف‌های Gantt/کارت‌های approvals/achievements تک‌ستونه |
| `600px` | فشرده‌سازی نهایی موبایل (topbar، stats تک‌ستونه) |

## حرکت (Motion)

- انیمیشن‌های تزئینی (`watermarkFloat`, `watermarkBreathe`, `loading` bar, `spin`) کوتاه و بی‌نهایت‌اند.
- تمام انیمیشن‌ها زیر `@media (prefers-reduced-motion: reduce)` در انتهای `globals.css` خاموش می‌شوند — این قانون را هنگام افزودن انیمیشن جدید نقض نکنید؛ اگر keyframe جدید ساختید نیازی به کار اضافه نیست چون قانون کلی (`*`) همه را می‌گیرد.

## سطح دسترسی (RBAC) در UI

`canManage(role)` در [app/lib/permissions.ts](app/lib/permissions.ts): هر نقش به‌جز `"user"` دسترسی مدیریت دارد. در UI:
- دکمه/فرم مدیریتی مخفی یا غیرفعال → بج `.readonly-badge` یا متن جایگزین با آیکن `Lock` (lucide-react) نمایش داده می‌شود.
- هرگز state مدیریتی را فقط با CSS مخفی نکنید؛ چک `canManage` باید در JSX شرطی باشد (نه فقط `display:none`) چون این یک مرز امنیتی UI است، نه فقط ظاهری — اما به‌خاطر داشته باشید enforcement واقعی باید سمت API هم باشد؛ این فقط لایهٔ نمایش است.
